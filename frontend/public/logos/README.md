Place finalized GameXS logo assets here (see the "Logo in application" task in `TODO.md`).

Referenced from the app as `/logos/<filename>` once added (e.g. `/logos/logo.svg` for a file
placed at `frontend/public/logos/logo.svg`).

Expected files, once available:
- `logo.svg` / `logo.png` — primary logo (header)
- `logo-mark.svg` — icon-only mark, no wordmark (favicon / loading screen source)
- `og-image.png` — 1200×630 social preview image

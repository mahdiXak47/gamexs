// ASCII alias for the Persian guide path. It is the canonical sitemap URL so
// monitoring tools and integrations do not need to handle an encoded route.
export const dynamic = "force-dynamic";
export { default, metadata } from "../راهنما/page";

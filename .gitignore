# Research/scraper raw downloads (HTML snapshots, API dumps used for building adapters)
scraper/research/raw/

# Scraper run artifacts (regenerable from a live/cached scrape, not source)
scraper/output/images/
scraper/output/*.log
scraper/output/*_offers.jsonl

# Python
__pycache__/
*.pyc
.venv/
venv/

# Node
node_modules/
.next/

# Env
.env
.env.local

# k8s Secrets (contain real credentials — never commit)
k8s/*-secret.yaml

# Claude Code internal state
.claude/worktrees/
.claude/settings.json

POSTGRES_USER=gamexs
POSTGRES_PASSWORD=gamexs
POSTGRES_DB=gamexs
POSTGRES_PORT=5432

DATABASE_URL=postgresql://gamexs:gamexs@localhost:5432/gamexs

# Telegram MTProto client credentials (https://my.telegram.org → API Development Tools).
# Used by the telegram_channel scraper. api_hash is a secret — never commit.
# TELEGRAM_PROXY: optional SOCKS5/HTTP proxy to reach Telegram from a restricted
# network, e.g. socks5://localhost:10809 (the repo's xray-proxy).
TELEGRAM_API_ID=
TELEGRAM_API_HASH=
TELEGRAM_PROXY=
